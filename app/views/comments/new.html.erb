<h3>Comment:</h1>
<%= render 'layouts/error_messages' %>
<%= form_for [@resource, @comment] do |f| %>
  <%= render partial: 'comments/form', locals: {f: f} %>
  <%= f.submit data: { disable_with: false }, class: "btn btn-default btn-space" %>
<% end %>
