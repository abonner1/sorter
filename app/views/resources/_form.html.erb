<%= f.label "Title:" %>
<%= f.text_field :title %><br />
<%= f.label "URL:" %>
<%= f.text_field :url %><br />
<%= f.label :description %>:
<%= f.text_area :description %><br />
<%= f.label "Select programming language:" %>
<%= f.collection_select(:language_id, Language.all, :id, :name, {:include_blank => "Please select"})%><br  />
<!-- <%= f.label "Tags:" %>
<%= f.collection_check_boxes :tag_ids, Tag.all, :id, :name%><br />
<%= f.fields_for :tags, @resource.tags.build do |tag_fields| %>
  <%= tag_fields.label "Or add a new topic:" %>
  <%= tag_fields.text_field :name %>
<% end %><br /> -->
